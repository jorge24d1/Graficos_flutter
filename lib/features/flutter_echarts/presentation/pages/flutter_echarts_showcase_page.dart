import 'package:flutter/material.dart';

class FlutterEchartsShowcasePage extends StatelessWidget {
  const FlutterEchartsShowcasePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('FlutterEcharts Showcase')),
      body: const Center(child: Text('FlutterEcharts Charts View')),
    );
  }
}
