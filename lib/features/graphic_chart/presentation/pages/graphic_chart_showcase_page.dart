import 'package:flutter/material.dart';

class GraphicChartShowcasePage extends StatelessWidget {
  const GraphicChartShowcasePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('GraphicChart Showcase')),
      body: const Center(child: Text('GraphicChart Charts View')),
    );
  }
}
