import 'package:flutter/material.dart';

class InteractiveChartShowcasePage extends StatelessWidget {
  const InteractiveChartShowcasePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('InteractiveChart Showcase')),
      body: const Center(child: Text('InteractiveChart Charts View')),
    );
  }
}
