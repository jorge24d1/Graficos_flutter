import 'package:flutter/material.dart';

class HighChartShowcasePage extends StatelessWidget {
  const HighChartShowcasePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('HighChart Showcase')),
      body: const Center(child: Text('HighChart Charts View')),
    );
  }
}
