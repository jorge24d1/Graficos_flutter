import 'package:flutter/material.dart';

class FlChartShowcasePage extends StatelessWidget {
  const FlChartShowcasePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('FlChart Showcase')),
      body: const Center(child: Text('FlChart Charts View')),
    );
  }
}
