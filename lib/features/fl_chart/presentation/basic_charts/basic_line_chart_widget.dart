import 'package:flutter/material.dart';

class FlChartBasicLineChartWidget extends StatelessWidget {
  const FlChartBasicLineChartWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('FlChartBasicLineChartWidget Placeholder'),
    );
  }
}
