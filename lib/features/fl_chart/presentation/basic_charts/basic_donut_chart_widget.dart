import 'package:flutter/material.dart';

class FlChartBasicDonutChartWidget extends StatelessWidget {
  const FlChartBasicDonutChartWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('FlChartBasicDonutChartWidget Placeholder'),
    );
  }
}
