import 'package:flutter/material.dart';

class SyncfusionChartShowcasePage extends StatelessWidget {
  const SyncfusionChartShowcasePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('SyncfusionChart Showcase')),
      body: const Center(child: Text('SyncfusionChart Charts View')),
    );
  }
}
